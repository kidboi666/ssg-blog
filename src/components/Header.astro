---
import Button from "./Button.astro";
import Link from "./Link.astro";
import MobileSideNavigation from "./MobileSideNavigation.astro";
---

<header
  class="fixed left-0 top-0 z-30 flex h-16 w-full items-center border-b border-zinc-200 bg-zinc-100 px-4"
>
  <div class="flex gap-2">
    <Link intent="primary" padding="md" href="/">
      <code>kidboi</code>
    </Link>
    <Button
      id="sidebar-button"
      intent="icon"
      padding="md"
      icon="menu"
      className="md:hidden"
    />
    <Link intent="icon" padding="md" href="/resume" icon="question" />
    <!-- <ThemeButton /> -->
  </div>
</header>
<div id="sidebar" class="-translate-x-full transition-all">
  <MobileSideNavigation />
</div>

<script is:inline>
  const button = document.querySelector("#sidebar-button");
  const sidebar = document.querySelector("#sidebar");

  button.addEventListener("click", () => {
    sidebar.classList.toggle("-translate-x-full");
  });
</script>
