---
import Button from "./Button";
import Link from "./Link";
import MobileSideNavigation from "./MobileSideNavigation.astro";
import ThemeButton from "./ThemeButton";
---

<div id="sidebar" class="hidden">
  <MobileSideNavigation />
</div>
<header
  class="fixed left-0 top-0 z-30 flex w-full items-center border-b border-zinc-200 bg-zinc-100 px-4 py-2 dark:border-zinc-900 dark:bg-var-accent-dark"
>
  <div class="flex w-full justify-between gap-2">
    <div class="flex gap-2">
      <Button
        id="sidebar-button"
        intent="icon"
        padding="md"
        icon="menu"
        className="md:hidden"
      />
      <Link intent="icon" padding="md" href="/" icon="home" />
      <Link intent="icon" padding="md" href="/resume" icon="question" />
    </div>
    <div class="flex gap-2">
      <Link
        intent="icon"
        padding="md"
        href="https://github.com/kidboi666"
        viewBox="0 0 18 18"
        icon="github"
      />
      <ThemeButton client:only="preact" />
    </div>
  </div>

  <script is:inline>
    const button = document.querySelector("#sidebar-button");
    const sidebar = document.querySelector("#sidebar");

    button.addEventListener("click", () => {
      sidebar.classList.toggle("hidden");
    });
  </script>
</header>
